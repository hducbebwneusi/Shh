services:
  - type: worker
    name: discord-email-bot
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python start_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false